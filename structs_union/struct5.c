/*Escribir una función que reciba 2 horas h1 y h2 (hora, minutos y segundos) y
devuelva la diferencia entre ambas (en horas, minutos y segundos). Se sabe que
h2 es posterior a h1.]*/

#include <stdio.h>

struct hora{
	int seg; 
	int min; 
	int hora; 
}


/* función que devuelve la diferencia entre dos horas */
struct hora diferencia(struct hora h1, struct hora h2){
	struct hora dif; 
	
	return dif; 
}

/*Programa principal */


int main(){




	return 0;
}